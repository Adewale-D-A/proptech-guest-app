/** @format */

import PendingRequestContainer from "@/container/pending-request";
import React from "react";

const PendingRequest = () => {
  return <PendingRequestContainer />;
};

export default PendingRequest;
